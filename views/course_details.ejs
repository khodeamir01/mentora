<!DOCTYPE html>
<html dir="rtl" lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta name="description" content="Eduleb - Education HTML Template" />
    <meta
      name="keywords"
      content="agency, business, corporate, creative, html5, modern, multipurpose, One Page, parallax, startup"
    />
    <title>Eduleb - آموزش قالب HTML</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inria+Sans:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/fonts/font-awesome.min.css" />
    <link rel="stylesheet" href="/fonts/themify-icons.css" />
    <link rel="stylesheet" href="/owlcarousel/css/owl.carousel.css" />
    <link rel="stylesheet" href="/owlcarousel/css/owl.theme.css" />
    <link rel="stylesheet" href="/css/jquery-simple-mobilemenu.css" />
    <link rel="stylesheet" href="/css/magnific-popup.css" />
    <link rel="stylesheet" href="/css/animate.css" />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body data-spy="scroll" data-offset="80">
    
    <div class="preloaders"><span class="loader"></span></div>
    <div id="navigation" class="navbar-light bg-faded site-navigation">
      <div class="container-fluid">
        <div class="row">
          <div class="col-20 align-self-center">
            <div class="site-logo">
              <a href="index.html"><img src="/img/logo.png" alt="" /></a>
            </div>
          </div>
          <div class="col-60 d-flex">
            <nav id="main-menu">
              <ul>

                <li><a href="/">صفحه اصلی</a></li>
                <li><a href="about.html">درباره</a></li>
                <li class="menu-item-has-children">
                  <a href="/course/all">دوره ها</a>
                  <ul>
                    <li><a href="course.html">دوره</a></li>
                    <li><a href="course_details.html">جزئیات دوره</a></li>
                  </ul>
                </li>
                <li class="menu-item-has-children">
                  <a href="#">صفحات</a>
                  <ul>
                    <li><a href="instructor.html">مربی</a></li>
                    <li><a href="ins_details.html">مشخصات مربی</a></li>
                    <li><a href="pricing.html">طرح قیمت گذاری</a></li>
                    <li><a href="faq.html">صفحه فک</a></li>
                    <li><a href="404.html">404</a></li>
                  </ul>
                </li>
                <li><a href="blog.html">مقالات</a></li>

              </ul>
            </nav>
          </div>
          <%if (user) { %>
          <p>خوش آمدید <%= user.name %></p>
          <% } else { %>
          <div class="col-20 d-none d-xl-block text-end align-self-center">
            <a href="/auth/login" class="header-btn">ورود</a
            ><a href="/auth/register" class="btn_one">ثبت نام</a>
          </div>
          <% } %>
          <ul class="mobile_menu">
            <li>
              <a href="#">صفحه اصلی</a>
              <ul class="sub-menu">
                <li><a href="index.html">صفحه اصلی 01</a></li>
                <li><a href="index2.html">صفحه اصلی 02</a></li>
              </ul>
            </li>
            <li><a href="about.html">در باره</a></li>
            <li>
              <a href="#">دوره</a>
              <ul class="sub-menu">
                <li><a href="course.html">دوره</a></li>
                <li><a href="course_details.html">جزئیات دوره</a></li>
              </ul>
            </li>
            <li>
              <a href="#">صفحات</a>
              <ul class="sub-menu">
                <li><a href="instructor.html">مربی</a></li>
                <li><a href="ins_details.html">مشخصات مربی</a></li>
                <li><a href="pricing.html">طرح قیمت گذاری</a></li>
                <li><a href="faq.html">صفحه فک</a></li>
                <li><a href="404.html">404</a></li>
              </ul>
            </li>
            <li>
              <a href="blog.html">وبلاگ</a>
              <ul class="sub-menu">
                <li><a href="blog.html">وبلاگ</a></li>
                <li><a href="blog_single.html">جزئیات وبلاگ</a></li>
              </ul>
            </li>
            <li><a href="contact.html">مخاطب</a></li>
          </ul>
        </div>
      </div>
    </div>
    <header class="content-hero">
      <!-- ذرات شناور -->
      <div class="particles">
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
          <div class="particle"></div>
      </div>
      
      <div class="hero-content">
          <span class="hero-badge">
              <i class="fa fa-folder"></i> <%= course.categoryID?.title || 'دوره' %>
          </span>
          <h1 class="hero-title"><%= course.name %></h1>
          <div class="hero-meta">
              <span>
                  <img src="<%= course.teacher?.avatar ?  course.teacher.avatar : '/img/defualtPic.png' %>" 
                       class="hero-avatar" alt="">
                  <strong><%= course.teacher?.name %></strong>
              </span>
              <span>
                  <i class="fa fa-calendar"></i> 
                  <%= new Date(course.updatedAt).toLocaleDateString('fa-IR') %>
              </span>
              <span>
                  <i class="fa fa-users"></i> <%= countStudents %> دانشجو
              </span>
              <span>
                  <i class="fa fa-trophy"></i> گواهینامه دارد
              </span>
          </div>
      </div>
  </header>
  

    <% if (course) { %>

    <section class="our_event section-padding">
      <div class="container">
        <div class="row">
          <!-- ستون سمت چپ (محتوی اصلی دوره) -->
          <div class="col-lg-8 col-sm-8 col-xs-12">
            <div class="single_event_single">
              <img
                src="/img/cover/<%= course.cover ? course.cover : '1.png' %>"
                alt="<%= course.name %>"
              />
              <h4><%= course.name %></h4>
              <div class="course-meta">
                <span class="meta-item"
                  ><i class="fa fa-calendar"></i> <%= new
                  Date(course.updatedAt).toLocaleDateString('fa-IR', { year:
                  'numeric', month: 'long', day: 'numeric' }) %></span
                >
                <span class="meta-item"
                  ><i class="fa fa-clock-o"></i> 7 روز</span
                >
                <span class="meta-item"
                  ><i class="fa fa-table"></i>
                  <strong>30 صندلی موجود</strong></span
                >
              </div>
              <div class="course-description">
                <p><%= course.description %></p>
              </div>
            </div>

            <!-- تب‌ها (بررسی اجمالی، برنامه تحصیلی، مربی، بررسی‌ها) -->
            <div class="course-details-content section-bg">
              <ul class="nav nav-tabs" role="tablist">
                <li class="nav-item">
                  <a
                    href="#overview"
                    class="nav-link active"
                    data-bs-toggle="tab"
                    >بررسی اجمالی</a
                  >
                </li>
                <li>
                  <a href="#curriculum" class="nav-link" data-bs-toggle="tab"
                    >جلسات </a
                  >
                </li>
                <li class="nav-item">
                  <a href="#instructor" class="nav-link" data-bs-toggle="tab"
                    >مربی</a
                  >
                </li>
                <li class="nav-item">
                  <a href="#reviews" class="nav-link" data-bs-toggle="tab"
                    >بررسی ها</a
                  >
                </li>
              </ul>
              <div class="tab-content" id="myTabContent">
                <div class="tab-pane show fade active" id="overview">
                  <div class="overview">
                    <p><%= course.description %></p>
                    <div class="details-buttons-area">
                      
                      <ul class="social-icons">
                        <li>
                          <a href="#0" class="active"
                            ><i class="fa fa-facebook"></i
                          ></a>
                        </li>
                        <li>
                          <a href="#0"><i class="fa fa-twitter"></i></a>
                        </li>
                        <li>
                          <a href="#0"><i class="fa fa-instagram"></i></a>
                        </li>
                      </ul>
                    </div>
                  </div>
                </div>
                <!-- برنامه تحصیلی -->
                <div class="tab-pane fade" id="curriculum">
                  <div class="overview">
                    <% if (sessions && sessions.length > 0) { %>
                      <div class="curriculum-list">
                        <% sessions.forEach((session, index) => { %>
                          <div class="curriculum-item">
                            <div class="curriculum-item-left">
                              <!-- شماره جلسه -->
                              <div class="session-number">
                                <%= session.order || index + 1 %>
                              </div>
                              
                              <div class="session-info">
                                <!-- عنوان جلسه -->
                                <h6 class="session-title"><%= session.title %></h6>
                                
                                <!-- توضیحات -->
                                <% if (session.description) { %>
                                  <p class="session-description"><%= session.description %></p>
                                <% } %>
                              </div>
                            </div>
                            
                            <div class="curriculum-item-right">
                              <!-- مدت زمان -->
                              <% if (session.duration) { %>
                                <span class="session-duration">
                                  <i class="fa fa-clock-o"></i> <%= session.duration %> دقیقه
                                </span>
                              <% } %>
                              
                              <!-- وضعیت رایگان/پولی -->
                              <span class="session-status <%= session.isFree ? 'free' : 'paid' %>">
                                <% if (session.isFree) { %>
                                  <i class="fa fa-unlock"></i> رایگان
                                <% } else { %>
                                  <i class="fa fa-lock"></i> نقدی
                                <% } %>
                              </span>
                              
                              <!-- دکمه نمایش ویدیو - فقط برای جلسات رایگان -->
                              <% if (session.isFree) { %>
                                <a href="#" class="watch-btn watch-session-btn" 
                                   data-video="/img/session/<%= session.video %>" 
                                   data-title="<%= session.title %>">
                                  <i class="fa fa-play"></i> مشاهده
                                </a>
                              <% } %>
                            </div>
                          </div>
                        <% }); %>
                      </div>
                      
                      <!-- اطلاعات کلی -->
                      <div class="curriculum-summary">
                        <div class="summary-item">
                          <h4 class="summary-number"><%= sessions.length %></h4>
                          <p class="summary-label">جلسه</p>
                        </div>
                        
                        <div class="summary-item">
                          <h4 class="summary-number"><%= sessions.filter(s => s.isFree).length %></h4>
                          <p class="summary-label">جلسه رایگان</p>
                        </div>
                      </div>
                      
                    <% } else { %>
                      <div class="empty-state">
                        <i class="fa fa-video-camera"></i>
                        <p>هنوز جلسه‌ای برای این دوره ثبت نشده است.</p>
                      </div>
                    <% } %>
                    

                  </div>
                </div>
                <div class="tab-pane fade" id="instructor">
                  <div class="overview text-center">
                    <div class="instructor-item">
                      <div class="instructor-thumb">
                        <a href="#0"
                          ><img
                            src="<%= course.teacher.avatar ? course.teacher.avatar : '/img/avatar/defualtPic' %>"
                            alt="instructor"
                        /></a>
                      </div>
                      <div class="instructor-content">
                        <h6 class="title">
                          <a href="#0"></a><%= course.teacher.name %>
                        </h6>
                        <span class="details">معلم</span>
                      </div>
                    </div>
                    <p>
<%= course.teacher.bio %>
                    </p>
                    <div class="details-buttons-area">
                      <ul class="social-icons justify-content-center w-100">
                        <li>
                          <a href="#0"><i class="fa fa-facebook"></i></a>
                        </li>
                        <li>
                          <a href="#0" class="active"
                            ><i class="fa fa-twitter"></i
                          ></a>
                        </li>
                        <li>
                          <a href="#0"><i class="fa fa-instagram"></i></a>
                        </li>
                      </ul>
                    </div>
                  </div>
                </div>
                <div class="tab-pane fade" id="reviews">
                  <div class="client-review">
                    <div class="review-comments">
                      <h6 class="review-title">
                        نظرات (<%= comments.length %>)
                      </h6>
                      <% if (!user) { %>
                      <div style="position: relative; margin: 25px 0; padding: 30px; background: white; border-radius: 20px; box-shadow: 0 10px 40px rgba(0,0,0,0.08); border: 1px solid #f0f0f0; text-align: center; overflow: hidden;">
                        <!-- پس‌زمینه دکوراتیو -->
                        <div style="position: absolute; top: -50px; right: -50px; width: 100px; height: 100px; background: linear-gradient(135deg, rgba(108,92,231,0.1), rgba(253,121,168,0.1)); border-radius: 50%;"></div>
                        <div style="position: absolute; bottom: -30px; left: -30px; width: 80px; height: 80px; background: linear-gradient(135deg, rgba(253,121,168,0.08), rgba(108,92,231,0.08)); border-radius: 50%;"></div>
                        
                        <div style="position: relative; z-index: 1;">
                          <div style="display: inline-block; background: linear-gradient(135deg, #6c5ce7, #fd79a8); padding: 15px 20px 15px 25px; border-radius: 50px; margin-bottom: 15px;">
                            <span style="color: white; font-size: 18px; font-weight: 600;">
                              <i class="fa fa-commenting" style="margin-left: 10px;"></i>
                              نظر شما مهم است!
                            </span>
                          </div>
                          
                          <p style="font-size: 15px; color: #2d3436; margin: 10px 0 20px;">
                            برای ثبت نظر و امتیازدهی به این دوره، لطفاً وارد شوید
                          </p>
                          
                          <div style="display: flex; gap: 12px; justify-content: center; flex-wrap: wrap;">
                            <a href="/auth/login" style="background: #6c5ce7; color: white; padding: 10px 30px; border-radius: 50px; text-decoration: none; font-weight: 500; font-size: 14px; transition: all 0.3s ease; display: inline-flex; align-items: center; gap: 8px;"
                               onmouseover="this.style.background='#5f3dc4'; this.style.transform='scale(1.05)'"
                               onmouseout="this.style.background='#6c5ce7'; this.style.transform='scale(1)'">
                              <i class="fa fa-arrow-right"></i>
                              ورود
                            </a>
                            <a href="/auth/register" style="background: white; color: #6c5ce7; padding: 10px 30px; border-radius: 50px; text-decoration: none; font-weight: 500; font-size: 14px; border: 2px solid #6c5ce7; transition: all 0.3s ease; display: inline-flex; align-items: center; gap: 8px;"
                               onmouseover="this.style.background='#6c5ce7'; this.style.color='white'"
                               onmouseout="this.style.background='white'; this.style.color='#6c5ce7'">
                              <i class="fa fa-user-plus"></i>
                              ثبت نام
                            </a>
                          </div>
                          
                          <div style="margin-top: 15px; display: flex; align-items: center; justify-content: center; gap: 15px; font-size: 12px; color: #b2bec3;">
                            <span><i class="fa fa-star" style="color: #fdcb6e;"></i> امتیازدهی</span>
                            <span><i class="fa fa-comment" style="color: #6c5ce7;"></i> نظر دادن</span>
                            
                          </div>
                        </div>
                      </div>  
                      <% } %>                  <ul class="review-contents">
                        <% if (comments.length > 0) { %> 
                          <%  comments.forEach(comment => { %>
                        <li
                          class="comment-item"
                          id="comment-<%= comment._id %>"
                        >
                          <div class="thumb">
                            <img
                              src="<%= comment.user.avatar ? '/img/avatar/' + comment.user.avatar : '/img/defualtPic.png' %>"
                              alt="user"
                            />
                          </div>
                          <div class="cont">
                            <h6 class="subtitle"><%= comment.user.name %></h6>
                            <div class="ratings cl-theme">
                              <% const rating = Number(comment.rating) || 0; %>
                              <% for (let i = 5; i >= 1; i--) { %>
                                  <span class="star-container">
                                    <i class="fa fa-star <%= i <= rating ? 'gold-star' : 'gray-star' %>"></i>
                                  </span>
                              <% } %>
                          </div>

                            <p><%= comment.content %></p>

                            <!-- فرم ارسال reply برای هر کامنت -->
                            <div
                              class="reply-form mt-3"
                              style="display: none"
                              id="replyForm-<%= comment._id %>"
                            >
                              <h6 class="review-title">پاسخ به این نظر</h6>
                              <form
                                action="/comments/<%= comment._id %>/reply"
                                method="POST"
                                class="row client-form align-items-center reply-form-element"
                              >
                                <!-- ستاره‌های قابل انتخاب برای reply -->
                                <div class="col-md-4 col-12 mb-3">
                                  <div class="rating-selection">
                                    <span class="rating-title"
                                      >امتیاز شما :</span
                                    >
                                    <div
                                      class="stars reply-stars"
                                      data-comment-id="<%= comment._id %>"
                                    >
                                      <% for (let i = 5; i >= 1; i--) { %>
                                      <i
                                        class="fa fa-star-o star-selectable reply-star"
                                        data-rating="<%= i %>"
                                      ></i>
                                      <% } %>
                                    </div>
                                    <!-- Input hidden برای ذخیره امتیاز انتخاب شده -->
                                    <input
                                      type="hidden"
                                      name="rating"
                                      class="reply-rating"
                                      value="0"
                                      data-comment-id="<%= comment._id %>"
                                    />
                                  </div>
                                </div>

                                <div class="col-md-12 col-12 mb-3">
                                  <textarea
                                    name="content"
                                    rows="3"
                                    class="form-control reply-textarea"
                                    placeholder="پاسخ خود را بنویسید..."
                                  ></textarea>
                                </div>

                                <div class="col-12">
                                  <button
                                    type="submit"
                                    class="custom-button rounded"
                                  >
                                    ارسال پاسخ
                                  </button>
                                  <button
                                    type="button"
                                    class="custom-button rounded cancel-reply"
                                    data-comment-id="<%= comment._id %>"
                                  >
                                    انصراف
                                  </button>
                                </div>
                              </form>
                            </div>

                            <!-- دکمه نمایش فرم reply -->
                            <% if (typeof user !== 'undefined' && user) { %>

                            <button
                              class="show-reply-form"
                              data-comment-id="<%= comment._id %>"
                            >
                              پاسخ به این نظر
                            </button>
                            <% } %>


                            <!-- دکمه حذف -->
                       <% if (user && (user._id.toString() === comment.user._id.toString() || (user.roles && user.roles.includes('ADMIN')))) { %>
                            <button
                              class="delete-comment-btn"
                              data-comment-id="<%= comment._id %>"
                              style="
                              margin-top: 8px;
                              padding: 4px 12px;
                              background-color: #ff4757;
                              color: white;
                              border: none;
                              border-radius: 4px;
                              cursor: pointer;
                              font-size: 12px;
                              transition: all 0.3s ease;
                            "
                            onmouseover="this.style.backgroundColor='#ff3838'"
                            onmouseout="this.style.backgroundColor='#ff4757'"
                          >
                            <i class="fa fa-trash"></i> حذف نظر
                            </button>
                            <% } %>
                          </div>
                        </li>

                        <!-- نمایش Reply‌ها -->
            <% if (comment.replies && comment.replies.length > 0) { %>
              <div class="replies-section">
                <% comment.replies.forEach((reply, replyIndex) => { %>
                  <li class="reply-item" id="reply-<%= reply._id %>" style="margin-right: 50px; list-style: none;">
                    <div class="thumb">
                      <img 
                        src="<%= reply.user && reply.user.avatar ? '/img/avatar/' + reply.user.avatar : '/img/defualtPic.png' %>" 
                        alt="user" 
                        style="width: 35px; height: 35px; border-radius: 50%;"
                      />
                    </div>
                    <div class="cont">
                      <h6 class="subtitle" style="font-size: 14px;">
                        <%= reply.user ? reply.user.name : 'کاربر ناشناس' %>
                        <small style="color: #999; font-size: 12px;">
                          <%= reply.createdAt ? new Date(reply.createdAt).toLocaleDateString('fa-IR') : '' %>
                        </small>
                      </h6>
                      
                      <!-- ستاره‌های Reply -->
                      <div class="ratings cl-theme">
                        <% const replyRating = parseInt(reply.rating) || 0; %>
                        <% for (let i = 5; i >= 1; i--) { %>
                          <span class="star-container">
                            <i class="fa fa-star <%= i <= replyRating ? 'gold-star' : 'gray-star' %>" style="font-size: 12px;"></i>                          </span>
                        <% } %>
                      </div>
                      
                      <p style="font-size: 13px;"><%= reply.content %></p>
                                <!-- دکمه حذف Reply -->
          <% if (user && (user._id.toString() === reply.user._id.toString() || (user.roles && user.roles.includes('ADMIN')))) { %>
            <button 
              class="delete-reply-btn" 
              data-comment-id="<%= comment._id %>" 
              data-reply-id="<%= reply._id %>"
              style="
                margin-top: 8px;
                padding: 4px 12px;
                background-color: #ff4757;
                color: white;
                border: none;
                border-radius: 4px;
                cursor: pointer;
                font-size: 12px;
                transition: all 0.3s ease;
              "
              onmouseover="this.style.backgroundColor='#ff3838'"
              onmouseout="this.style.backgroundColor='#ff4757'"
            >
              <i class="fa fa-trash"></i> حذف پاسخ
            </button>
          <% } %>
                    </div>
                  </li>
                <% }); %>
              </div>
            <% } %>
                        <% }); %> <% } else { %>
                        <p>هنوز نظری برای این دوره ثبت نشده است.</p>
                        <% } %>
                      </ul>
                    </div>

                    <!-- فرم ارسال نظر -->
                    <% if (typeof user !== 'undefined' && user) { %>

                    <div class="review-form mt-5">
                      <h6 class="review-title">
                        نظر خود درباره این دوره را بنویسید
                      </h6>
                      <!-- اکشن فرم را به روت ثبت نظر هدایت کنید -->
                      <form
                        action="/comments/<%= course.href %>/create"
                        method="POST"
                        class="row client-form align-items-center"
                        id="reviewForm"
                      >
                        <input
                          type="hidden"
                          name="productId"
                          value="<%= course._id %>"
                        />

                        <!-- ستاره‌های قابل انتخاب برای امتیازدهی -->
                        <div class="col-md-4 col-12 mb-3">
                          <div class="rating-selection">
                            <span class="rating-title">امتیاز شما :</span>
                            <div class="stars">
                              <% for (let i = 5; i >= 1; i--) { %>
                              <i
                                class="fa fa-star-o star-selectable"
                                data-rating="<%= i %>"
                              ></i>
                              <% } %>
                            </div>
                            <!-- Input hidden برای ذخیره امتیاز انتخاب شده -->
                            <input
                              type="hidden"
                              name="rating"
                              id="selectedRating"
                              value="0"
                            />
                          </div>
                        </div>

                        <div class="col-md-12 col-12 mb-3">
                          <textarea
                            name="content"
                            id="commentInput"
                            rows="5"
                            class="form-control"
                            placeholder="اینجا پیام را تایپ کنید"
                          ></textarea>
                        </div>
                        <div class="col-12">
                          <button type="submit" class="custom-button rounded">
                            ارسال
                          </button>
                        </div>
                      </form>
                    </div>
                    <% } %>

                  </div>
                </div>
              </div>
            </div>
            <!-- پایان تب‌ها -->
          </div>
          <!-- پایان ستون سمت چپ -->

          <!-- ستون سمت راست (ویژگی‌های دوره، قیمت، دوره‌های مرتبط، دسته‌بندی‌ها و شبکه‌های اجتماعی) -->
          <div class="col-lg-4 col-sm-4 col-xs-12">
            <div class="course_features">
              <h3>ویژگی‌های دوره</h3>
              <ul>
                <li>
                  <i class="fa fa-calendar"></i>
                  <span
                    >وضعیت دوره:
                    <b
                      ><% if (course.status === "published") { %> منتشر شده <% }
                      else { %> در حال برگزاری <% } %></b
                    >
                  </span>
                </li>
                <li>
                  <i class="fa fa-user"></i>
                  <span>مدرس: <b><%= course.teacher.name %></b></span>
                </li>
                <li>
                  <i class="fa fa-users"></i>
                  <span> تعداد کل دانشجو ها <b><%= countStudents %> </b></span>
                </li>
                <li>
                  <i class="fa fa-trophy"></i>
                  <span>گواهینامه: <b>دارد</b></span>
                </li>
              </ul>
            </div>
            <% const isStudent = locals.isUserRegistered || false; %>
            <% if (!isStudent) { %>
            <div class="event_info_price">
              <h4>قیمت - <%= course.price %> تومان</h4>
            </div>
            <% } %>

<!-- دکمه‌های ثبت نام و افزودن به سبد خرید -->
<div class="event_info_register">
  <% if (user) { %>
    <% const isStudent = locals.isUserRegistered || false; %>
    <% if (isStudent) { %>
      <!-- قبلاً خریده -->
      <div style="background: linear-gradient(135deg, #2ecc71, #27ae60); color: white; padding: 16px 20px; border-radius: 16px; text-align: center; font-size: 16px; font-weight: 700; box-shadow: 0 10px 20px rgba(46,204,113,0.3);">
        <i class="fa fa-check-circle"></i> شما دانشجوی این دوره هستید
      </div>

    <% } else { %>
    <!-- اگر کاربر لاگین کرده -->
    <button class="btn_one add-to-cart-btn" 
    data-course-id="<%= course._id.toString() %>" 
    data-teacher-id="<%= course.teacher && course.teacher._id ? course.teacher._id.toString() : '' %>"
    style="width: 100%; border: none; cursor: pointer;">
<i class="fa fa-cart-plus"></i> افزودن به سبد خرید
</button>
    
    <a class="btn_one" href="/cart" style="margin-top: 10px; display: block; text-align: center; background: #28a745;">
      <i class="fa fa-shopping-cart"></i> مشاهده سبد خرید
    </a>
    <% } %>

  <% } else { %>
    <!-- اگر کاربر لاگین نکرده -->
    <a class="btn_one" href="/auth/login">
      <i class="fa fa-sign-in"></i> برای ثبت نام وارد شوید
    </a>
  <% } %>
</div>


            <div class="related_course">
              <h3>دیگر دوره های مرتبط</h3>
              <% if (relatedCourses && relatedCourses.length > 0) { %> <%
              relatedCourses.forEach(function(relCourse) { %>
              <div class="single_rc">
                <div class="rc_img">
                  <img
                    src="/img/cover/<%= relCourse.cover %>"
                    alt="<%= relCourse.name %>"
                  />
                </div>
                <i class="fa fa-star"></i><i class="fa fa-star"></i>
                <h4>
                  <a href="/course/<%= relCourse.href %>"
                    ><%= relCourse.name %></a
                  >
                </h4>
                <span><%= relCourse.price %> تومان</span>
              </div>
              <% }); %> <% } else { %>
              <p>دوره مرتبطی یافت نشد.</p>
              <% } %>
            </div>

            <div class="sidebar-post">
              <div class="sidebar_title"><h4>دسته بندی ها</h4></div>
              <% if (locals.categories && categories.length > 0) { %>
              <div class="single_category">
                <ul>
                  <% categories.forEach(function(category) { %>
                  <li>
                    <a href="/categories/<%= category.href %>">
                      <%= category.title %>
                    </a>
                  </li>
                  <% }); %>
                </ul>
              </div>
              <% } else { %>
              <p>دسته بندی موجود نیست</p>
              <% } %>
            </div>

            <div class="sidebar-post">
              <div class="sidebar_title"><h4>ما را دنبال کنید</h4></div>
              <div class="single_social">
                <ul>
                  <li>
                    <div class="social_item b_facebook">
                      <a href="#" title="facebook"
                        ><i class="fa fa-facebook"></i
                        ><span class="item-list">150 هزار لایک</span></a
                      >
                    </div>
                  </li>
                  <li>
                    <div class="social_item b_twitter">
                      <a href="#" title="twitter"
                        ><i class="fa fa-twitter"></i
                        ><span class="item-list">138 هزار دنبال کننده</span></a
                      >
                    </div>
                  </li>
                  <li>
                    <div class="social_item b_youtube">
                      <a href="#" title="youtube"
                        ><i class="fa fa-youtube"></i
                        ><span class="item-list">90 هزار مشترک</span></a
                      >
                    </div>
                  </li>
                  <li>
                    <div class="social_item b_pinterest">
                      <a href="#" title="pinterest"
                        ><i class="fa fa-pinterest"></i
                        ><span class="item-list">35 هزار دنبال کننده</span></a
                      >
                    </div>
                  </li>
                  <li>
                    <div class="social_item b_tumblr">
                      <a href="#" title="rss"
                        ><i class="fa fa-tumblr"></i
                        ><span class="item-list">901 فالوور</span></a
                      >
                    </div>
                  </li>
                  <li>
                    <div class="social_item b_rss">
                      <a href="#" title="rss"
                        ><i class="fa fa-rss"></i
                        ><span class="item-list">411 فالوور</span></a
                      >
                    </div>
                  </li>
                </ul>
              </div>
            </div>
          </div>
          <!-- پایان ستون سمت راست -->
        </div>
        <!-- پایان row -->
      </div>
      <!-- پایان container -->
    </section>
    <!-- پایان section -->

    <% } else { %>
    <div class="col-12 text-center">
      <p>در حال حاضر دوره‌ای برای نمایش وجود ندارد.</p>
    </div>
    <% } %>

    <div class="footer section-padding">
      <div class="container">
        <div class="row">
          <div class="col-lg-3 col-sm-6 col-xs-12">
            <div class="single_footer">
              <a href="index.html"><img src="/img/logo.png" alt="" /></a>
              <p>
                لورم ایوم متن ساختگی با تولید آسان نامفهوم از صنعت چاپ با
                استفاده از طراحان گرافیک است. چاپگرها و
              </p>
              <div class="social_profile">
                <ul>
                  <li>
                    <a href="#" class="f_facebook"
                      ><i class="ti-facebook" title="Facebook"></i
                    ></a>
                  </li>
                  <li>
                    <a href="#" class="f_twitter"
                      ><i class="ti-twitter" title="Twitter"></i
                    ></a>
                  </li>
                  <li>
                    <a href="#" class="f_instagram"
                      ><i class="ti-instagram" title="Instagram"></i
                    ></a>
                  </li>
                  <li>
                    <a href="#" class="f_linkedin"
                      ><i class="ti-linkedin" title="LinkedIn"></i
                    ></a>
                  </li>
                </ul>
              </div>
            </div>
          </div>
          <div class="col-lg-2 col-sm-6 col-xs-12">
            <div class="single_footer">
              <h4>درباره ادولب</h4>
              <ul>
                <li><a href="#">درباره ما</a></li>
                <li><a href="#">ثبت نام مدرس</a></li>
                <li><a href="#">معلم شدن</a></li>
                <li><a href="#">همه مربیان</a></li>
                <li><a href="#">سوال پرسیده شده</a></li>
                <li><a href="#">با ما تماس بگیرید</a></li>
              </ul>
            </div>
          </div>
          <div class="col-lg-2 col-sm-6 col-xs-12">
            <div class="single_footer">
              <h4>دوره محبوب</h4>
              <ul>
                <li><a href="#">توسعه</a></li>
                <li><a href="#">هنر و طراحی</a></li>
                <li><a href="#">طراحی بصری</a></li>
                <li><a href="#">طراحی گرافیک</a></li>
                <li><a href="#">بازرسی کد</a></li>
                <li><a href="#">بازاریابی دیجیتال</a></li>
              </ul>
            </div>
          </div>
          <div class="col-lg-3 col-sm-6 col-xs-12">
            <div class="single_footer">
              <h4>اطلاعات تماس</h4>
              <div class="sf_contact">
                <span class="ti-map"></span>
                <p>2570 کوادرا خیابان ویکتوریا، نیویورک، کانادا</p>
              </div>
              <div class="sf_contact">
                <span class="ti-mobile"></span>
                <p>+989928616443</p>
              </div>
              <div class="sf_contact">
                <span class="ti-email"></span>
                <p>amirsalarkhormaie@gmail.com</p>
              </div>
            </div>
          </div>
          <div class="col-lg-2 col-sm-6 col-xs-12">
            <div class="single_footer">
              <h4>دانلود برنامه</h4>
              <p>
                برنامه ما را از فروشگاه برنامه و فروشگاه google play دانلود
                کنید.
              </p>
              <a href="index.html"
                ><img src="/img/google-play.jpg" class="foot_img" alt="" /></a
              ><a href="index.html"
                ><img src="/img/app-store.jpg" class="foot_img" alt=""
              /></a>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="foot_copy">
      <div class="footer_copyright"><p>© 2024. All Rights Reserved.</p></div>
    </div>
    <script src="/js/sweetAlert2.js"></script>
    <script src="/js/jquery-1.12.4.min.js"></script>
    <script src="/bootstrap/js/bootstrap.min.js"></script>
    <script src="/js/modernizr-2.8.3.min.js"></script>
    <script src="/js/jquery-simple-mobilemenu.js"></script>
    <script src="/owlcarousel/js/owl.carousel.min.js"></script>
    <script src="/js/jquery.magnific-popup.min.js"></script>
    <script src="/js/jquery.inview.min.js"></script>
    <script src="/js/scrolltopcontrol.js"></script>
    <script src="/js/wow.min.js"></script>
    <script src="/js/scripts.js"></script>
    <script>
      // ============= افزودن به سبد خرید =============
document.querySelector('.add-to-cart-btn')?.addEventListener('click', async function() {
  const btn = this;
  const courseId = btn.getAttribute('data-course-id');
  const teacherId = btn.getAttribute('data-teacher-id');
  
  // غیرفعال کردن دکمه برای جلوگیری از کلیک مجدد
  btn.disabled = true;
  btn.innerHTML = '<i class="fa fa-spinner fa-spin"></i> در حال افزودن...';
  
  try {
    const response = await fetch('/cart/add', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        courseId: courseId,
        teacherId: teacherId,
        quantity: 1
      })
    });
    
    const data = await response.json();
    
    if (data.success) {
      // تغییر ظاهر دکمه
      btn.innerHTML = '<i class="fa fa-check"></i> اضافه شد ✓';
      btn.classList.add('added');
      
      // نمایش پیام موفقیت
      Swal.fire({
        icon: 'success',
        title: 'به سبد خرید اضافه شد! 🎉',
        text: 'دوره با موفقیت به سبد خرید شما اضافه شد',
        showCancelButton: true,
        confirmButtonText: 'مشاهده سبد خرید',
        cancelButtonText: 'ادامه خرید',
        confirmButtonColor: '#667eea',
        cancelButtonColor: '#6c757d',
        reverseButtons: true
      }).then((result) => {
        if (result.isConfirmed) {
          window.location.href = '/cart';
        }
      });
      
      // برگشت دکمه به حالت اول بعد از 3 ثانیه
      setTimeout(() => {
        btn.innerHTML = '<i class="fa fa-cart-plus"></i> افزودن به سبد خرید';
        btn.classList.remove('added');
        btn.disabled = false;
      }, 3000);
      
    } else {
      // خطا
      btn.disabled = false;
      btn.innerHTML = '<i class="fa fa-cart-plus"></i> افزودن به سبد خرید';
      
      Swal.fire({
        icon: 'warning',
        title: 'توجه',
        text: data.error || 'این دوره قبلاً به سبد خرید اضافه شده است',
        confirmButtonText: 'باشه',
        confirmButtonColor: '#667eea',
        footer: '<a href="/cart">مشاهده سبد خرید</a>'
      });
    }
  } catch (error) {
    // خطای شبکه
    btn.disabled = false;
    btn.innerHTML = '<i class="fa fa-cart-plus"></i> افزودن به سبد خرید';
    
    Swal.fire({
      icon: 'error',
      title: 'خطا',
      text: 'خطا در ارتباط با سرور. لطفاً دوباره تلاش کنید',
      confirmButtonText: 'باشه'
    });
  }
});
      // نسخه ساده - فقط برای لینک‌های مستقیم
// ============= مودال نمایش ویدیو =============
document.querySelectorAll('.watch-session-btn').forEach(btn => {
  btn.addEventListener('click', function(e) {
    e.preventDefault();
    
    const videoUrl = this.getAttribute('data-video');
    const title = this.getAttribute('data-title');
    const sessionNumber = this.closest('.curriculum-item')?.querySelector('.session-number')?.textContent?.trim();
    
    console.log('Video URL:', videoUrl); // دیباگ
    
    // چک کردن خالی نبودن لینک
    if (!videoUrl || videoUrl === 'undefined' || videoUrl === 'null') {
      Swal.fire({
        icon: 'warning',
        title: 'ویدیو موجود نیست',
        text: 'متاسفانه ویدیوی این جلسه هنوز بارگذاری نشده است',
        confirmButtonText: 'باشه',
        confirmButtonColor: '#667eea'
      });
      return;
    }
    
    Swal.fire({
      title: sessionNumber ? `جلسه ${sessionNumber} - ${title}` : title,
      html: `
        <div style="position: relative; background: #000; border-radius: 8px; overflow: hidden;">
          <video 
            controls 
            autoplay 
            playsinline
            style="width: 100%; max-height: 70vh; display: block;"
          >
            <source src="${videoUrl}" type="video/mp4">
            مرورگر شما از پخش این ویدیو پشتیبانی نمی‌کند.
          </video>
        </div>
      `,
      width: '900px',
      showCloseButton: true,
      showConfirmButton: false,
      background: '#000',
      customClass: {
        popup: 'video-swal-popup'
      },
      didOpen: () => {
        // اضافه کردن دکمه بستن سفارشی
        const closeBtn = document.createElement('button');
        closeBtn.innerHTML = '✕';
        closeBtn.style.cssText = `
          position: absolute;
          top: 10px;
          left: 10px;
          z-index: 9999;
          background: rgba(255,255,255,0.2);
          border: none;
          color: white;
          width: 36px;
          height: 36px;
          border-radius: 50%;
          cursor: pointer;
          font-size: 18px;
          display: flex;
          align-items: center;
          justify-content: center;
          transition: all 0.3s ease;
        `;
        closeBtn.onmouseover = () => closeBtn.style.background = 'rgba(255,0,0,0.7)';
        closeBtn.onmouseout = () => closeBtn.style.background = 'rgba(255,255,255,0.2)';
        closeBtn.onclick = () => Swal.close();
        
        const popup = Swal.getPopup();
        if (popup) popup.appendChild(closeBtn);
      },
      willClose: () => {
        // قطع ویدیو موقع بستن
        const video = document.querySelector('.swal2-html-container video');
        if (video) {
          video.pause();
          video.currentTime = 0;
        }
      }
    });
  });
});
      // ============= حذف Reply با SweetAlert2 =============
document.querySelectorAll(".delete-reply-btn").forEach((button) => {
  button.addEventListener("click", async function () {
    const commentId = this.getAttribute("data-comment-id");
    const replyId = this.getAttribute("data-reply-id");
    
    const result = await Swal.fire({
      title: "حذف پاسخ",
      text: "آیا مطمئن هستید که می‌خواهید این پاسخ را حذف کنید؟",
      icon: "warning",
      showCancelButton: true,
      confirmButtonText: "بله، حذف کن",
      cancelButtonText: "انصراف",
      confirmButtonColor: "#d33",
      cancelButtonColor: "#3085d6",
      reverseButtons: true
    });

    if (result.isConfirmed) {
      try {
        Swal.fire({
          title: "در حال حذف پاسخ...",
          allowOutsideClick: false,
          didOpen: () => {
            Swal.showLoading();
          },
        });

        const response = await fetch(`/comments/${commentId}/reply/${replyId}`, {
          method: "DELETE",
          headers: {
            "Content-Type": "application/json",
          },
        });

        const data = await response.json();

        if (data.success) {
          Swal.fire({
            icon: "success",
            title: "حذف شد!",
            text: "پاسخ با موفقیت حذف شد",
            timer: 2000,
            showConfirmButton: false
          });

          // حذف عنصر Reply از DOM
          const replyElement = document.getElementById(`reply-${replyId}`);
          if (replyElement) {
            replyElement.remove();
            
            // بررسی کن آیا replies دیگه‌ای باقی مونده
            const repliesSection = replyElement.closest(".replies-section");
            if (repliesSection && repliesSection.children.length === 0) {
              repliesSection.remove();
            }
          }
        } else {
          Swal.fire({
            icon: "error",
            title: "خطا",
            text: data.error || "خطا در حذف پاسخ",
            confirmButtonText: "باشه",
            confirmButtonColor: "#667eea"
          });
        }
      } catch (error) {
        console.error("خطا:", error);
        Swal.fire({
          icon: "error",
          title: "خطا",
          text: "خطا در ارتباط با سرور",
          confirmButtonText: "باشه",
          confirmButtonColor: "#667eea"
        });
      }
    }
  });
});
      document.addEventListener("DOMContentLoaded", function () {
        
        // ============= مدیریت ستاره‌های کامنت اصلی =============
        const mainStars = document.querySelectorAll("#reviewForm .star-selectable");
        const mainRatingInput = document.getElementById("selectedRating");
        setupStarSystem(mainStars, mainRatingInput);
      
        // ============= مدیریت ستاره‌های Reply =============
        const replyStarGroups = document.querySelectorAll(".reply-stars");
        replyStarGroups.forEach((starGroup) => {
          const commentId = starGroup.getAttribute("data-comment-id");
          const replyStars = starGroup.querySelectorAll(".reply-star");
          const replyRatingInput = document.querySelector(
            `.reply-rating[data-comment-id="${commentId}"]`
          );
          setupStarSystem(replyStars, replyRatingInput);
        });
      
        // ============= تابع اصلی مدیریت ستاره‌ها =============
        function setupStarSystem(stars, ratingInput) {
    function highlightStars(rating) {
        const starsArray = Array.from(stars);
        starsArray.forEach((star) => {
            const starRating = parseInt(star.getAttribute("data-rating"));
            if (starRating <= rating) {  // ← اینو عوض کن
                star.classList.remove("fa-star-o");
                star.classList.add("fa-star");
                star.style.color = "#FFD700";
            } else {
                star.classList.remove("fa-star");
                star.classList.add("fa-star-o");
                star.style.color = "#FFD700";
            }
        });
    }

    function setRating(rating) {
        ratingInput.value = rating;
        highlightStars(rating);
    }

    stars.forEach((star) => {
        star.addEventListener("click", function () {
            const rating = parseInt(this.getAttribute("data-rating"));
            setRating(rating);
        });

        star.addEventListener("mouseenter", function () {
            const rating = parseInt(this.getAttribute("data-rating"));
            highlightStars(rating);
        });

        star.addEventListener("mouseleave", function () {
            const currentRating = parseInt(ratingInput.value);
            if (currentRating === 0) {
                resetStars();
            } else {
                highlightStars(currentRating);
            }
        });
    });
}
      
        // ============= مدیریت نمایش/مخفی کردن فرم Reply =============
        const showReplyButtons = document.querySelectorAll(".show-reply-form");
        const cancelReplyButtons = document.querySelectorAll(".cancel-reply");
      
        showReplyButtons.forEach((button) => {
          button.addEventListener("click", function () {
            const commentId = this.getAttribute("data-comment-id");
            const replyForm = document.getElementById(`replyForm-${commentId}`);
      
            document.querySelectorAll(".reply-form").forEach((form) => {
              form.style.display = "none";
            });
      
            if (replyForm) {
              replyForm.style.display = "block";
            }
          });
        });
      
        cancelReplyButtons.forEach((button) => {
          button.addEventListener("click", function () {
            const commentId = this.getAttribute("data-comment-id");
            const replyForm = document.getElementById(`replyForm-${commentId}`);
      
            if (replyForm) {
              const textarea = replyForm.querySelector(".reply-textarea");
              const ratingInput = replyForm.querySelector(".reply-rating");
              
              // اگر متنی نوشته شده، تأیید بگیر
              if (textarea && textarea.value.trim() !== "") {
                Swal.fire({
                  title: "انصراف از پاسخ؟",
                  text: "متن نوشته شده پاک می‌شود",
                  icon: "question",
                  showCancelButton: true,
                  confirmButtonText: "بله، منصرف شدم",
                  cancelButtonText: "ادامه نوشتن",
                  confirmButtonColor: "#d33",
                  cancelButtonColor: "#3085d6",
                  reverseButtons: true
                }).then((result) => {
                  if (result.isConfirmed) {
                    hideReplyForm(replyForm, textarea, ratingInput);
                  }
                });
              } else {
                hideReplyForm(replyForm, textarea, ratingInput);
              }
            }
          });
        });
      
        function hideReplyForm(replyForm, textarea, ratingInput) {
          replyForm.style.display = "none";
          if (textarea) textarea.value = "";
          if (ratingInput) ratingInput.value = "0";
          
          const stars = replyForm.querySelectorAll(".reply-star");
          stars.forEach((star) => {
            star.classList.remove("fa-star");
            star.classList.add("fa-star-o");
            star.style.color = "#FFD700";
          });
        }
      
        // ============= ارسال فرم Reply با SweetAlert2 =============
        document.querySelectorAll(".reply-form-element").forEach((form) => {
          form.addEventListener("submit", async function (e) {
            e.preventDefault();
      
            const commentId = this.closest(".reply-form").id.replace("replyForm-", "");
            const ratingInput = this.querySelector(".reply-rating");
            const contentInput = this.querySelector(".reply-textarea");
      
            const rating = parseInt(ratingInput?.value || 0);
            const content = contentInput?.value?.trim() || "";
      
            // اعتبارسنجی با SweetAlert2
            if (rating === 0) {
              Swal.fire({
                icon: "warning",
                title: "اخطار",
                text: "لطفاً امتیاز خود را انتخاب کنید",
                confirmButtonText: "باشه",
              });
              return;
            }
      
            if (content === "") {
              Swal.fire({
                icon: "warning",
                title: "اخطار",
                text: "لطفاً پاسخ خود را بنویسید",
                confirmButtonText: "باشه",
              });
              return;
            }
      
            try {
              // نمایش لودینگ
              Swal.fire({
                title: "در حال ثبت پاسخ...",
                allowOutsideClick: false,
                didOpen: () => {
                  Swal.showLoading();
                },
              });
      
              const response = await fetch(`/comments/${commentId}/reply`, {
                method: "POST",
                headers: {
                  "Content-Type": "application/json",
                },
                body: JSON.stringify({
                  rating: rating,
                  content: content,
                }),
              });
      
              const data = await response.json();
      
              if (data.success) {
                Swal.fire({
                  icon: "success",
                  title: "پاسخ ثبت شد! 🎉",
                  text: "پاسخ شما با موفقیت ارسال شد",
                  confirmButtonText: "رفتن به صفحه اصلی",
                  showCancelButton: true,
                  cancelButtonText: "ماندن در صفحه",
                  reverseButtons: true,
                }).then((result) => {
                  if (result.isConfirmed) {
                    window.location.href = "/";
                  } else {
                    location.reload();
                  }
                });
              } else {
                Swal.fire({
                  icon: "error",
                  title: "خطا",
                  text: data.error || "مشکلی در ثبت پاسخ پیش آمد",
                  confirmButtonText: "باشه",
                });
              }
            } catch (error) {
              console.error("Error:", error);
              Swal.fire({
                icon: "error",
                title: "خطا",
                text: "خطا در ارتباط با سرور",
                confirmButtonText: "باشه",
              });
            }
          });
        });
      
        // ============= ارسال فرم کامنت اصلی با SweetAlert2 =============
        document.getElementById("reviewForm")?.addEventListener("submit", async (e) => {
          e.preventDefault();
      
          const rating = parseInt(mainRatingInput.value);
          const content = document.getElementById("commentInput").value.trim();
      
          if (rating === 0) {
            Swal.fire({
              icon: "warning",
              title: "اخطار",
              text: "لطفاً امتیاز خود را انتخاب کنید",
              confirmButtonText: "باشه",
            });
            return;
          }
      
          if (content === "") {
            Swal.fire({
              icon: "warning",
              title: "اخطار",
              text: "لطفاً نظر خود را بنویسید",
              confirmButtonText: "باشه",
            });
            return;
          }
      
          try {
            Swal.fire({
              title: "در حال ثبت نظر...",
              allowOutsideClick: false,
              didOpen: () => {
                Swal.showLoading();
              },
            });
      
            const response = await fetch("/comments/<%= course.href %>/create", {
              method: "POST",
              headers: {
                "Content-Type": "application/json",
              },
              body: JSON.stringify({
                rating: rating,
                content: content,
              }),
            });
      
            const data = await response.json();
      
            if (data.success) {
              Swal.fire({
                icon: "success",
                title: "موفقیت",
                text: "نظر شما با موفقیت ثبت شد",
                confirmButtonText: "رفتن به صفحه اصلی",
                showCancelButton: true,
                cancelButtonText: "ماندن در صفحه",
                reverseButtons: true,
              }).then((result) => {
                if (result.isConfirmed) {
                  window.location.href = "/";
                } else {
                  location.reload();
                }
              });
            } else {
              Swal.fire({
                icon: "error",
                title: "خطا",
                text: data.error || "مشکلی در ثبت نظر پیش آمد",
                confirmButtonText: "باشه",
              });
            }
          } catch (error) {
            console.error("Error:", error);
            Swal.fire({
              icon: "error",
              title: "خطا",
              text: "خطا در ارتباط با سرور",
              confirmButtonText: "باشه",
            });
          }
        });
      
        // ============= حذف کامنت با SweetAlert2 =============
        document.querySelectorAll(".delete-comment-btn").forEach((button) => {
          button.addEventListener("click", async function () {
            const commentId = this.getAttribute("data-comment-id");
            
            const result = await Swal.fire({
              title: "حذف کامنت",
              text: "آیا مطمئن هستید که می‌خواهید این کامنت را حذف کنید؟",
              icon: "warning",
              showCancelButton: true,
              confirmButtonText: "بله، حذف کن",
              cancelButtonText: "انصراف",
              confirmButtonColor: "#d33",
              cancelButtonColor: "#3085d6",
              reverseButtons: true
            });
      
            if (result.isConfirmed) {
              try {
                Swal.fire({
                  title: "در حال حذف...",
                  allowOutsideClick: false,
                  didOpen: () => {
                    Swal.showLoading();
                  },
                });
      
                const response = await fetch(`/comments/${commentId}`, {
                  method: "DELETE",
                  headers: {
                    "Content-Type": "application/json",
                  },
                });
      
                const data = await response.json();
      
                if (data.success) {
                  Swal.fire({
                    icon: "success",
                    title: "حذف شد!",
                    text: "کامنت با موفقیت حذف شد",
                    timer: 2000,
                    showConfirmButton: false
                  });
      
                  const commentElement = document.getElementById(`comment-${commentId}`);
                  if (commentElement) {
                    commentElement.remove();
                  }
      
                  const reviewContents = document.querySelector(".review-contents");
                  if (reviewContents && reviewContents.children.length === 0) {
                    reviewContents.innerHTML = "<p>هنوز نظری برای این دوره ثبت نشده است.</p>";
                  }
                } else {
                  Swal.fire({
                    icon: "error",
                    title: "خطا",
                    text: data.error || "خطا در حذف کامنت",
                    confirmButtonText: "باشه",
                  });
                }
              } catch (error) {
                console.error("خطا:", error);
                Swal.fire({
                  icon: "error",
                  title: "خطا",
                  text: "خطا در ارتباط با سرور",
                  confirmButtonText: "باشه",
                });
              }
            }
          });
        });
      
      });
      </script>
  </body>
</html>
